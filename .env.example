# Copy to .env.local for local app development. Staging and production
# secrets should live in EAS/GitHub/operator secret stores, not in git.

# --- Environment ---------------------------------------------------
# One of: local, test, staging, production
APP_ENV=local
COMPOSE_FILE=infra/docker-compose.yaml

# Public API base URL. Required for local, staging, and production app builds.
EXPO_PUBLIC_API_BASE_URL= # http://localhost:8081
PUBLIC_APP_BASE_URL= # http://localhost:8081

# Public legal URLs shown in app settings.
EXPO_PUBLIC_PRIVACY_POLICY_URL=https://example.com/privacy
EXPO_PUBLIC_TERMS_URL=https://example.com/terms

# Optional app identity overrides. app.config.ts derives environment-specific
# local/staging identifiers when these are omitted.
EXPO_APP_NAME=
EXPO_SCHEME=
IOS_BUNDLE_IDENTIFIER=

# --- Development ---------------------------------------------------
# Pass PORT to Makefile commands when another worktree owns Metro's default
# 8081 port, for example: make ios PORT=8090
EXPO_WITH_ROCKETSIM_CONNECT=

# --- Clerk ---------------------------------------------------------
# Publishable key — safe to ship in the app. Non-production uses pk_test_*. Production uses pk_live_*.
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_

# Secret key — server-only, used by API Routes to verify sessions
CLERK_SECRET_KEY=sk_test_

# --- Resend --------------------------------------------------------
# Server-only, used by API Routes to send invitation emails
RESEND_API_KEY=
RESEND_FROM_ADDRESS=invites@yourdomain.com

# --- PostHog -------------------------------------------------------
# Public project token and host. Events/logs are tagged with APP_ENV.
POSTHOG_PROJECT_TOKEN=
POSTHOG_HOST=https://us.i.posthog.com

# --- Postgres / PowerSync ------------------------------------------
# Source Postgres (server + drizzle migrate). NEVER commit the real value.
DATABASE_URL=
# PowerSync client connector endpoint (consumed in PR-B/PR-C2).
EXPO_PUBLIC_POWERSYNC_URL=
# Compose-only vars for infra/docker-compose.yaml (local) and
# infra/compose.staging.yaml (staging server .env).
PG_DATABASE_USER=
PG_DATABASE_NAME=
PG_DATABASE_PASSWORD=
PG_STORAGE_DATABASE_USER=
PG_STORAGE_DATABASE_NAME=
PG_STORAGE_DATABASE_PASSWORD=
PS_DATA_SOURCE_URI=
PS_STORAGE_SOURCE_URI=
PS_PORT=
# Clerk frontend-API JWKS URL for the env's Clerk instance (PowerSync token verification).
# Dev e.g. https://<your-dev-subdomain>.clerk.accounts.dev/.well-known/jwks.json
PS_CLERK_JWKS_URI=
