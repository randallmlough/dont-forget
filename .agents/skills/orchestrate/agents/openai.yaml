interface:
  display_name: "Orchestrate"
  short_description: "Drive feature tasks through workers"
  default_prompt: "Use $orchestrate to drive this feature from state.json through worker delegation, review, verification, and final completion."
